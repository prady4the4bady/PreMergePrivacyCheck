{
  "name": "premerge-privacy-check",
  "version": "1.0.0",
  "description": "GitHub Action to scan PR diffs for secrets and PII",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "jest",
    "lint": "eslint src/**/*.js",
    "lint:fix": "eslint src/**/*.js --fix"
  },
  "keywords": ["github-action", "security", "privacy", "secrets", "pii"],
  "author": "prady4the4bady",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@octokit/rest": "^19.0.7"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "eslint": "^8.35.0",
    "jest": "^29.5.0"
  },
  "engines": {
    "node": ">=16"
  }
}